.venv/
__pycache__/
*.pyc

# Source archives and derived data are reproducible from the pipeline.
data/raw/
data/landing/
*.duckdb
*.duckdb.wal

dbt/target/
dbt/dbt_packages/
dbt/logs/
.dagster/

# Machine-local dbt state. package-lock.yml is deliberately NOT ignored: it
# pins package versions and belongs in version control.
dbt/.user.yml
dbt/package-lock.concurrent-update-lock

.DS_Store
.vscode/
.idea/
